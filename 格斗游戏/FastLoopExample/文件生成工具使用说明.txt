

打包：
 
  将要打包的多张图片拖进工具内，工具会自动加载。 自定义好 .tg 文件的名字，然后点生成。 就能在文件夹下生成一个相应的.tg文件，代码里面直接读取就能获得资源数据。

  注： 程序由读取.tg文件获得资源时，生成的texture的名字都为原图片的名字（去掉扩展名），在使用Tetxuremanager.Get("Name")的时候使用原图片名字。 


解包：
   把.tg文件拖进工具里面，点还原。文件就生成到工具的本地文件夹下了。



   工具功能尚不完善，切忌重复操作（易发生不可控bug，导致资源全废），最好实现功能以后重新运行一次。